spring:
  application:
    name: gateway # 服务名称
  profiles:
    active: local
  cloud:
    nacos:
      server-addr: 192.168.200.130:8848 # nacos地址
      discovery:
        namespace:
          group: DEFAULT_GROUP
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置

          - dataId: shared-logs.yaml
